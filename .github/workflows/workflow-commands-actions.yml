name: Action Commands
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # ENV Vars
      - name: Test composite
        uses: ./.github/actions/action.yml@main
        with:
          skip-tests: true